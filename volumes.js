// function torusVolume(majorRadius, minorRadius) {
//   return (Math.PI * (minorRadius ** 2)) * (2 * Math.PI * majorRadius)
// }

module.exports = function (majorRadius, minorRadius) {
  return (Math.PI * (minorRadius ** 2)) * (2 * Math.PI * majorRadius)
}
